# 最小依赖，纯 Python CLI demo 就够跑。
# 与 pyproject.toml 保持一致，方便不想用 uv/hatch 的同学直接 pip install。
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
openai>=1.40
langgraph>=0.2.50
pydantic>=2.8
pydantic-settings>=2.4
sse-starlette>=2.1
python-dotenv>=1.0
