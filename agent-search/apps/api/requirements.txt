# 最小依赖，纯 Python CLI demo 就够跑。
# 与 pyproject.toml 保持一致，方便不想用 uv/hatch 的同学直接 pip install。
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
openai>=1.40
langgraph>=0.2.50
pydantic>=2.8
pydantic-settings>=2.4
sse-starlette>=2.1
python-dotenv>=1.0
# 本地 RAG：pickle + numpy 点积（最简版）
numpy>=1.24
# 可选：EMBED_BACKEND=local 时需要（本地嵌入，不调外部 API）
# 装完首次用会下载 HuggingFace 权重到 ~/.cache/huggingface/
# pip install sentence-transformers
# sentence-transformers>=2.7
# 可选：scripts/build_index.py --pdf 需要 pypdf；默认 md/txt 不装也行
# pypdf>=4.0
